{
  "name": "energy-data-governance-chat-api",
  "private": true,
  "type": "module",
  "scripts": {
    "build:site": "npm run build:chat-config",
    "build:knowledge": "python build_knowledge.py",
    "build:chat-config": "node scripts/generate-chat-config.mjs",
    "dev": "node scripts/dev-local.mjs",
    "dev:site": "python -m http.server 8080",
    "dev:api": "vercel dev --listen 3000"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@vercel/node": "^5.1.14",
    "typescript": "^5.8.3"
  }
}
